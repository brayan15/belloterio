// import { getNavService } from './dataApi'

jest.mock('./dataApi')

describe('Akamai getImages', () => {
  it('should make a request when we get images', done => {
    /* const spy = jest.fn()
    getNavService.mockImplementation(() => Promise.resolve(spy))
    getNavService().then(() => {
      expect(spy).toBeCalled()
      done()
    }) */
    done()
  })
})
